import { useRouter } from "next/router";

const Notice = () => {
  const router = useRouter();
  //   const { id } = router.query;

  //   console.log("id", id);
  return <p>Post: </p>;
};

export default Notice;
